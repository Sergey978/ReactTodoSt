import React from 'react';
import './search-panel.css';

const SearchPanel = () => {

    const  searchText = "Serching text"
    return <input placeholder={searchText} />
}
  export default SearchPanel;